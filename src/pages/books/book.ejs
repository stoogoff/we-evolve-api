<% layout('../layouts/main') %>

<div>
	<article itemscope itemtype="https://schema.org/Book">
		<tag-list :tags="book.tags" />
		<header>
			<h1 itemprop="name"><%= it.data.title %></h1>
			<% if(it.data.series) { %>
			<p class="uppercase text-sm">
				Part <%= it.data.part %> of the <strong><%= it.data.series %></strong> series.
			</p>
			<% } %>
		</header>
		<div>
			<publish-date :item="book" />
			<div class="overflow-hidden">
				<%~ include('../components/article-image', {
					image: it.data.image,
				}) %>
				<%~ include('../components/prose-block', {
					summary: it.data.summary,
					content: it.data.content,
				}) %>
			</div>
			<% if(it.data.urls) { %>
			<aside class="border-t border-gray-200 mt-6 pt-4 px-2">
				<p class="text-xl">More information, including purchasing options, is available at the following sites:</p>
				<ul class="bullet">
					<% Object.keys(it.data.urls).forEach(key => { %>
					<li>
						<a href="<%= it.data.urls[key] %>" class="link" itemprop="url"><%= key %></a>
					</li>
					<% }) %>
				</ul>
			</aside>
			<% } %>
		</div>
	</article>
</div>
